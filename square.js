// testing jest package
// https://jestjs.io/docs/getting-started

function square(a) {
    return a * a
}

function squareRoot(num) {
    return Math.sqrt(num)
}

module.exports = {
    square,
    squareRoot
}